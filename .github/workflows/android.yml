name: Android Instaladores

on:
  workflow_dispatch:
  push:
    tags:
      - "android-v*"

permissions:
  contents: write

jobs:
  build-android:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: bem-instalado/frontend

    steps:
      - name: Baixar código
        uses: actions/checkout@v6

      - name: Configurar Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.22
          cache: npm
          cache-dependency-path: bem-instalado/frontend/package-lock.json

      - name: Configurar Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Preparar Android SDK
        shell: bash
        run: |
          set +o pipefail
          yes | "$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager" \
            "platforms;android-36" \
            "build-tools;36.0.0" >/dev/null
          set -o pipefail

      - name: Instalar dependências
        run: npm ci

      - name: Gerar aplicativo web dos instaladores
        run: npm run build:android:web

      - name: Sincronizar projeto Android
        run: npx cap sync android

      - name: Preparar assinatura permanente
        shell: bash
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          test -n "$ANDROID_KEYSTORE_BASE64"
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "$RUNNER_TEMP/instalapro-upload.jks"

      - name: Compilar Android App Bundle
        working-directory: bem-instalado/frontend/android
        env:
          ANDROID_KEYSTORE_PATH: ${{ runner.temp }}/instalapro-upload.jks
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          chmod +x gradlew
          ./gradlew bundleRelease --no-daemon

      - name: Preparar arquivo para download
        shell: bash
        run: |
          cp android/app/build/outputs/bundle/release/app-release.aab InstalaPro-Instaladores.aab
          sha256sum InstalaPro-Instaladores.aab > InstalaPro-Instaladores.aab.sha256

      - name: Salvar APK e AAB na execução
        uses: actions/upload-artifact@v6
        with:
          name: InstalaPro-Instaladores
          path: |
            bem-instalado/frontend/InstalaPro-Instaladores.aab
            bem-instalado/frontend/InstalaPro-Instaladores.aab.sha256
          if-no-files-found: error

      - name: Remover chave temporária
        if: always()
        shell: bash
        run: rm -f "$RUNNER_TEMP/instalapro-upload.jks"
